-- need a report which uses legacy logic to determine students who should be considered legacy but arent, vice versa

-- also a report to run rules and compare with students groups (i.e. run GR/PRL rule, compare to those in that group)

select * from rorcmpl;
-- select rorcmpl_sql_statement from rorcmpl;
select * from rorslct;
